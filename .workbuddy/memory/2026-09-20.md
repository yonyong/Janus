# 2026-09-20

## 工作台新 UI 落地（用户确认原型后实施）
- 删除 WorkflowSteps.tsx 及 wb-flow/wb-lite 样式；Workbench.tsx 重写：顶部单栏（+阶段 chip），
  去掉标准/轻量模式切换（后端 mode 字段保留不读写）。
- 新增 components/FlowCommands.tsx（6 条指令跨阶段编号、commandDone 按产出判定、onUse=changeStage+loadDraft，
  导出 buildFlowCommands/verifyCasesPrompt）；components/FileWorkArea.tsx（竖向分类 rail + 横向子页签，
  stage→cat 单向联动；编码分类只留文件/改动）。
- 分栏拖拽（320px~62%、双击复位、body.wb-dragging）+ 收起按钮；wb-body 改 flex，wb-solo 删除。
- styles.css：删 wb-flow/wb-lite 块，新增 wb-split/wb-collapse-btn/wfa-*/flow-cmds(fc-*)/wb-stage-chip。
- 同步 ui_smoke_workbench.mjs / ui_smoke_preview.mjs：clickStep→clickRail(.wfa-rail-item)，
  flowLoadedExpr=.fc-row>=6，还原阶段改为还原分类。
- 验证：tsc 通过、vite build 通过、后端 241/241、ui_smoke_workbench/preview/auth 三套全绿（8000 端口现有服务直接服务新 dist，无需重启）。

## 视觉修复三连（用户截图反馈）
- wfa-rail 角标改挂图标右上角（Badge 包 icon + offset），不再绝对定位盖文字。
- 编辑器只有一小条的根因：.wfa 缺 height:100%，高度链断裂 → doc-editor flex:1 失效。
  已补 .wfa{height:100%} 和 .wfa-main{min-height:0}。
- FlowCommands 默认收起（useState(false)）；ui_smoke_workbench 断言默认 .closed，展开后再查 .fc-row=6。
- 用户反馈：右栏对话区顶部的「需求」预览卡（标题+描述，.req-brief）不要展示——已从 RequirementPane 删除
  （showBrief/requirement prop 一并清掉，.req-brief/.req-desc 样式删除）。tsc/build/UI 冒烟通过。

## web 前端依赖损坏修复
- 症状：vite dev 报 antd/@ant-design/icons/dayjs/.../xlsx 等 "could not be resolved"，且实际加载的是 vite 8.3.0
  （与 package.json ^5.4.0、package-lock 锁的 5.4.21 不符）——node_modules 是坏的半装状态。
- 修复：rm -rf node_modules 后按 lockfile 重装（npm install），重装后 vite 回到 5.4.21，oxc/jsx 弃用告警与
  could-not-resolve 全部消失，dev 干净启动。
- ⚠️ 坑：本受限网络下 `npm install` 会卡在最后的 `npm audit` 上报约 50 分钟不退出（deps 其实早已装完）。
  以后一律用 `npm install --no-audit --no-fund` 避免卡死。

## 晚间会话：前端 5173 + 后端 8000 整栈重启（21:02-21:36）
- 用户误以为"项目页空白=代码没加载"。澄清：coding-agent-platform 是管理壳，项目/agent 需手动登记，
  且 项目列表按 ?token= 过滤（无 token 前端不请求、后端 401）；当前 DB 全空属初始状态。
- 用户两轮之间把项目更新为最新版（antd 富 UI：Workbench/FlowCommands/CasePane/RequirementDocPane 等），
  新 package.json 含 antd/@ant-design/icons/dayjs/marked/dompurify/highlight.js/xlsx/docx-preview。
  首轮 install 跑在旧 package.json 上，缺新依赖 → vite HMR 报 `Failed to resolve import "antd"` 崩溃。
  重跑 `npm install --no-audit --no-fund` 后依赖齐备，dev 干净启动（5173）。
- 后端 8000 中途停了，前端代理 /api/admin/state 报 ECONNREFUSED。用 .venv\Scripts\python.exe start.py 重启后端，8000 恢复。
- 端口处理：清掉占用 5173 的僵死旧 vite 进程 + 杀掉 5174 临时实例，最终前端独占 5173、后端 8000。
- 当前运行态（截至本会话）：vite dev @5173（task glvp9i）、uvicorn @8000（task k0wwtt）均后台存活。
